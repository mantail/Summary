#include "Auxillary.h"
#include <iostream>
#include <stdlib.h>
using namespace std;
// SortCriteria is a variable that keeps track of the criteria the user wishes to sort by
// SearchCriteria is a variable that stores all the information that the user wishes to sort by
SearchCriteria sortCriteria = LAST_NAME;
vector<SearchCriteria> searchCriteria;
